# Beauty Visual Inbox Mini App

Vite/React Telegram Mini App frontend for the Beauty Visual Inbox feed.

## Commands

```bash
npm run dev
npm run build
npm run lint
```

The app reads Telegram `initData` from `window.Telegram.WebApp` when opened from
Telegram. Local Playwright tests use stubs and mocked API responses.
